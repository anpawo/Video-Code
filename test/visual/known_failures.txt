# Visual scenes that already fail on main, with the reason they are tolerated.
# The pre-push hook blocks on any failure NOT listed here — so this file is the
# difference between "we know" and "we broke it just now".
#
# Remove a line the moment its scene passes again: a stale entry silently
# disarms the gate for that scene.
#
# All three date from before 2026-07-28 and are glyph-related — text-stroke and
# markdown render text, gradient-holes renders letter contours with holes.
# Verified pre-existing by rebuilding a clean HEAD and re-running the suite.
text-stroke
gradient-holes
markdown
