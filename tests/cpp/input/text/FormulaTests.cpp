#include <gtest/gtest.h>

#include "input/text/Formula.hpp"

TEST(FormulaHeader, Compiles)
{
    // Header is empty / minimal, just ensure it can be included in the test build.
    EXPECT_TRUE(true);
}
